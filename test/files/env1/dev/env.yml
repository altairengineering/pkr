#default_meta:
#required_meta:
#default_features:
#context_template_dir

driver:
  docker_compose:
    compose_file: 'templates/docker-compose.yml.template'

containers:
  backend:
    dockerfile: backend.dockerfile
