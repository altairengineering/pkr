containers:
  container1:
    build:
      subfolder: contexts/container1
      dockerfile: file1.dockerfile
      requires:
        - src: $SRC_PATH/templates/dockerfiles/build/file_build.sh
          dst: build.sh
    run:
      subfolder: mounts/container1
      requires:
        - src: $SRC_PATH/templates/dockerfiles/run/file_run.sh
          dst: run.sh
